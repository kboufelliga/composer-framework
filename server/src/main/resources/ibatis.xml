<?xml version="1.0" encoding="UTF-8" ?>

<!DOCTYPE sqlMapConfig
    PUBLIC "-//ibatis.apache.org//DTD SQL Map Config 2.0//EN"
    "http://ibatis.apache.org/dtd/sql-map-config-2.dtd">

<sqlMapConfig>

    <properties resource="datasource.properties" />

    <settings
        useStatementNamespaces="false"
        cacheModelsEnabled="true"
        enhancementEnabled="true"
        lazyLoadingEnabled="true"
    />

    <transactionManager type="JDBC" >
        <dataSource type="DBCP">
            <property name="JDBC.Driver" value="${driver}"/>
            <property name="JDBC.ConnectionURL" value="${url}"/>
            <property name="JDBC.Username" value="${user.name}"/>
            <property name="JDBC.Password" value="${user.password}"/>

            <property name="iniyialSize" value="5"/>
            <property name="maxActive" value="30"/>
            <property name="maxIdle" value="20"/>
            <property name="maxWait" value="60000"/>
            <property name="validationQuery" value="select 1"/>
            <property name="testOnBorrow" value="true"/>

            <property name="testWhileIdle" value="true"/>
            <property name="timeBetweenEvictionRunsMillis" value="720000"/>
            <property name="minEvictableIdleTimeMillis" value="360000"/>
        </dataSource>
    </transactionManager>

    <sqlMap resource="payment_mysql.xml" />
</sqlMapConfig> 